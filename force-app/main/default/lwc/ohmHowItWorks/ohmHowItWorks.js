import { LightningElement } from 'lwc';

/** Static explainer: the loop, the stack, and the footprint math as inline SVG in the app's palette. */
export default class OhmHowItWorks extends LightningElement {}
